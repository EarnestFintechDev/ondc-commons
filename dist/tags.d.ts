export declare class Tags {
    descriptor: string;
    list: {
        descriptor?: {
            code: string;
        };
        value: string;
    }[];
    constructor(descriptor: string, list: {
        descriptor?: {
            code: string;
        };
        value: string;
    }[]);
}
