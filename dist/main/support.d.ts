import { Context } from "../context";
export declare class SupportApi {
    context: Context;
    message: {
        ref_id: string;
    };
    constructor(context: Context, message: {
        ref_id: string;
    });
}
